import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    // mode: mode,
    primary: {
      main: '#1760a5',
    },
    secondary: {
      main: '#15c630'
    },
    black: {
      main: '#000',
    }
  },
})
